spring.application.name=LRE_Project

# ==========================================================
# POSTGRESQL CONFIG (Your VPS DB)
# ==========================================================
spring.datasource.url=jdbc:postgresql://195.35.45.56:5432/local_real_estate_database
spring.datasource.username=postgres
spring.datasource.password=KHAJa#0715
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=5858


# ==========================================================
# MULTIPART (File Upload) SETTINGS
# ==========================================================
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=250MB


# ==========================================================
# FILE STORAGE PATHS (For VPS / Production)
# ==========================================================
# Use a clean path on your VPS (adjust if you stored uploads elsewhere)
file.upload.image-dir=/root/bellary_uploads/images/
file.upload.video-dir=/root/bellary_uploads/videos/

# ==========================================================
# BASE URL (IMPORTANT: fixes Mixed Content issue)
# ==========================================================
# Replace domain with your backend's HTTPS domain
app.base-url=https://api.bellaryinfotech.com


# ==========================================================
# SERVER CONFIG
# ==========================================================
# Ensure your backend runs under HTTPS reverse proxy (Nginx / Apache)
# Example Nginx reverse proxy ensures SSL and correct port mapping